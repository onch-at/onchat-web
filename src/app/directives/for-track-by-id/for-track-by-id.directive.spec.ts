import { ForTrackByIdDirective } from './for-track-by-id.directive';

describe('ForTrackByIdDirective', () => {
  it('should create an instance', () => {
    const directive = new ForTrackByIdDirective();
    expect(directive).toBeTruthy();
  });
});
