import { VirtualScrollPatchDirective } from './virtual-scroll-patch.directive';

describe('VirtualScrollPatchDirective', () => {
  it('should create an instance', () => {
    const directive = new VirtualScrollPatchDirective();
    expect(directive).toBeTruthy();
  });
});
