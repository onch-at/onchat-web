import { ActiveClassDirective } from './active-class.directive';

describe('ActiveClassDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveClassDirective();
    expect(directive).toBeTruthy();
  });
});
