import { ImageSizeDirective } from './image-size.directive';

describe('ImageSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageSizeDirective();
    expect(directive).toBeTruthy();
  });
});
