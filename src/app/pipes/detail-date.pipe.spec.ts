import { DetailDatePipe } from './detail-date.pipe';

describe('DetailDatePipe', () => {
  it('create an instance', () => {
    const pipe = new DetailDatePipe();
    expect(pipe).toBeTruthy();
  });
});
