import { MemberRolePipe } from './member-role.pipe';

describe('MemberRolePipe', () => {
  it('create an instance', () => {
    const pipe = new MemberRolePipe();
    expect(pipe).toBeTruthy();
  });
});
