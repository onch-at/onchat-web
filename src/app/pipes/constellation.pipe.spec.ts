import { ConstellationPipe } from './constellation.pipe';

describe('ConstellationPipe', () => {
  it('create an instance', () => {
    const pipe = new ConstellationPipe();
    expect(pipe).toBeTruthy();
  });
});
